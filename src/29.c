#include <stdio.h>

int main() {
    int x = 10;
    if (x > 5) {
        printf("The value of x is greater than 5.\n");
    } else {
        printf("The value of x is not greater than 5.\n");
    }

    return 0;
}
